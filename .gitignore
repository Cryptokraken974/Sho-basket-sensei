# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environments
.venv/
.env

# Build artifacts
build/
dist/
*.egg-info/

# Local data (SQLite db, stored/transcoded video assets)
data/

# CV model weights / checkpoints — downloaded artifacts, not source.
# (The SAM 2 checkpoint is ~184MB; GitHub rejects files over 100MB.)
models/
*.pt
*.pth
*.onnx
